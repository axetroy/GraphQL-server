{
  "name": "graphql-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --single-quote --list-different 'app/**/*.ts'",
    "start": "ts-node-dev --fast --respawn app/server.ts",
    "build:dev": "rimraf ./build && tsc app/*.ts app/**/*.ts --outDir ./build --sourceMap --allowJs --strict",
    "build": "rimraf ./build && tsc app/*.ts app/**/*.ts --outDir ./build --sourceMap --allowJs --strict --noUnusedLocals --noUnusedParameters --noImplicitReturns",
    "server": "pm2 ./build/server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/axetroy/graphQL-example.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/axetroy/graphQL-example/issues"
  },
  "homepage": "https://github.com/axetroy/graphQL-example#readme",
  "dependencies": {
    "@types/express-graphql": "^0.0.33",
    "express": "^4.15.3",
    "express-graphql": "^0.6.6",
    "graphql": "^0.10.3",
    "lodash": "^4.17.4",
    "moment": "^2.18.1",
    "mongoose": "^4.11.1",
    "uuid": "^3.1.0"
  },
  "devDependencies": {
    "@types/es6-promise": "^0.0.32",
    "@types/express": "^4.0.36",
    "@types/graphql": "^0.9.4",
    "@types/lodash": "^4.14.68",
    "@types/mongoose": "^4.7.18",
    "@types/node": "^8.0.11",
    "@types/uuid": "^3.0.0",
    "rimraf": "^2.6.1"
  }
}
